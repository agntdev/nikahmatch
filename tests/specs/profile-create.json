[
  {
    "name": "Create profile",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Start the profile creation wizard (language + rules + fields)"
            }
          }
        ],
        "send": {
          "callback": "profile:create"
        }
      }
    ]
  }
]
